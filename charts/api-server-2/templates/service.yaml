apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-server-2.fullname" . }}
  labels:
    {{- include "api-server-2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-server-2.selectorLabels" . | nindent 4 }}
