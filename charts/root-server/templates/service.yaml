apiVersion: v1
kind: Service
metadata:
  name: {{ include "root-server.fullname" . }}
  labels:
    {{- include "root-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "root-server.selectorLabels" . | nindent 4 }}
