apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "root-server.fullname" . }}-test-connection"
  labels:
    {{- include "root-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "root-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
