apiVersion: v1
kind: Service
metadata:
  name: {{ include "goroutine-server.fullname" . }}
  labels:
    {{- include "goroutine-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goroutine-server.selectorLabels" . | nindent 4 }}
