apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "goroutine-server.fullname" . }}-test-connection"
  labels:
    {{- include "goroutine-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "goroutine-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
